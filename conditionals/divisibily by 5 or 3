#include <iostream>
using namespace std;
int main()
{
    int x;
    cout << "enter the no:-  ";
    cin >> x;

    if (x % 5 == 0 and x % 3 == 0)
        cout << "divisible by 5 and 3";
    else if (x % 5 == 0)
        cout << "divisiblee by 5";
    else if (x % 3 == 0)
        cout << "divisible by 3";
    else
        cout << "not divisible by 5 or 3";
}
